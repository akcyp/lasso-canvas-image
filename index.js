/**
 * Create Canvas Config
 * @param {HTMLCanvasElement} canvas
 * @param {(polygon: string) => void | boolean} onChange
 */
function createLasso (canvas, onChange) {

}

module.exports = createLasso;
